{{ relation.source_id }}:{{ relation.source_port.value }} {% if relation.label %}-[{{ relation.label | mermaid_quote }}]->{% else %}-->{% endif %} {{ relation.target_port.value }}:{{ relation.target_id }}{{ "\n" -}}
