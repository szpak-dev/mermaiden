{% macro expression(item) -%}{% if item.kind == "terminal" %}{{ item.label | mermaid_quote }}{% elif item.kind == "nonterminal" %}{{ item.label }}{% elif item.kind == "special" %}? {{ item.label }} ?{% elif item.kind == "sequence" %}{% for child in item.elements %}{{ expression(child) }}{% if not loop.last %} {% endif %}{% endfor %}{% elif item.kind == "alternative" %}{% for child in item.elements %}{{ expression(child) }}{% if not loop.last %} | {% endif %}{% endfor %}{% elif item.kind == "optional" %}[ {{ expression(item.elements[0]) }} ]{% elif item.kind == "repetition" %}{ {{ expression(item.elements[0]) }} }{% elif item.kind == "group" %}( {{ expression(item.elements[0]) }} ){% endif %}{%- endmacro %}
railroad-ebnf-beta{{ "\n" -}}
{% for rule in diagram.root_elements %}{{ rule.id | mermaid_id("r") }} = {{ expression(rule) }} ;{{ "\n" -}}{% endfor %}
